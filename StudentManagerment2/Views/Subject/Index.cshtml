@model IEnumerable<StudentManagerment2.Models.Subject>

@{
    ViewBag.Title = "Danh sách các môn học";
}

<h2 class="text-center text-primary">Danh sách các môn học</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-primary mb-3" })
</p>

<table class="table table-striped table-bordered table-hover">
    <thead class="table-dark">
        <tr class="text-center">
            <th>@Html.DisplayNameFor(model => model.SubjectName)</th>
            <th>@Html.DisplayNameFor(model => model.Description)</th>
            <th>Hành động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.SubjectName)</td>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td class="text-center">
                    @Html.ActionLink("Sửa", "Edit", new { id = item.Id }, new { @class = "btn btn-warning btn-sm" })
                    @Html.ActionLink("Xóa", "Delete", new { id = item.Id }, new { @class = "btn btn-danger btn-sm" })
                </td>
            </tr>
        }
    </tbody>
</table>
